---
- name: Copy wallpaper image file
  ansible.builtin.copy:
    src: "wallpaper.png"
    dest: "{{ ansible_env.HOME }}/.wallpaper.png"

- name: Set wallpaper on GNOME
  ansible.builtin.command: "gsettings set org.gnome.desktop.background picture-uri 'file:///{{ ansible_env.HOME }}/.wallpaper.png'"
# disable_services/tasks/main.yml
---
- name: Stop and disable services
  ansible.builtin.systemd:
    name: '{{ item }}'
    enabled: no
    state: stopped
  loop_control:
    loop_var: item
  become: true
  ignore_errors: true
  with_items:
    - tor
    - nxserver
    - cups
    # - ssh